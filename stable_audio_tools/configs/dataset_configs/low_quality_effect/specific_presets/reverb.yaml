# @package dataset.low_quality_effect.strong_reverb
mode: set

# Available SoX effects: https://manpages.ubuntu.com/manpages/lunar/en/man1/sox.1.html#effects
transform_type: sox_audio_effect

effects:
- name: 'strong_reverb'
  params:
  # Strong reverb: high reverberance (e.g., 70), long HF decay (e.g., 2s), large room scale (e.g., 100), long pre-delay (e.g., 50ms)
  # Adjusting parameters for a strong effect, ensuring it's noticeable.
  # Example: reverb [reverberance (0-100)] [hf-damping (0-100)] [room-scale (0-100)] [stereo-depth (0-100)] [pre-delay (ms)] [wet-gain (dB)]
  - "reverb 70 50 100 100 50 0"
  gain: 0.7 # Adjust gain to avoid excessive loudness due to reverb

external_sounds: []
